# X Drafts

## Single Post

Your coding agents do not read the same repo.

Rulefall shows which AGENTS.md / CLAUDE.md / Cursor / Copilot instructions reach an agent—when, and why—as a startup → discovery → edit waterfall.

Local-only. No upload, model call, or telemetry. OSS.

<https://github.com/LyraZeta/rulefall>

## Short Thread

**1/3** Your coding agents do not read the same repo. The same instruction file can be loaded by one agent, deferred until a target is known by another, and ignored by a third.

**2/3** I built Rulefall to make that lifecycle visible across Codex, Claude Code, Cursor, and Copilot. Import locally, choose a cwd + target, then scrub startup → discovery → edit. Every event explains “why.”

**3/3** It is a conformance simulator, not a hidden-prompt inspector or compliance claim. No backend, uploads, telemetry, or model calls. Demo + source: <https://github.com/LyraZeta/rulefall>

Use either the single post or the thread, not both on the same launch. Add a real screenshot or recording and alt text describing the waterfall.
